[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @agentvisa/localhost-demo... build"]

[start]
cmd = "node packages/localhost-demo/scripts/serve-static.mjs"
